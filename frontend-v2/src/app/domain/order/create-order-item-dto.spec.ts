import { CreateOrderItemDto } from './create-order-item-dto';

describe('CreateOrderItemDto', () => {
  it('should create an instance', () => {
    expect(new CreateOrderItemDto()).toBeTruthy();
  });
});
