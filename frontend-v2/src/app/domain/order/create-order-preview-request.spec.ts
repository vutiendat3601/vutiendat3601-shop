import { CreateOrderPreviewRequest } from './create-order-preview-request';

describe('CreateOrderPreviewRequest', () => {
  it('should create an instance', () => {
    expect(new CreateOrderPreviewRequest()).toBeTruthy();
  });
});
