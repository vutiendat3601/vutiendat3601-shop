import { CreateOrderRequest } from './create-order-request';

describe('CreateOrderRequest', () => {
  it('should create an instance', () => {
    expect(new CreateOrderRequest()).toBeTruthy();
  });
});
