import { UpdateOrderStatus } from './update-order-status';

describe('UpdateOrderStatus', () => {
  it('should create an instance', () => {
    expect(new UpdateOrderStatus()).toBeTruthy();
  });
});
