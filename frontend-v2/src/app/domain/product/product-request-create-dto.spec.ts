import { ProductRequestCreateDto } from './product-request-create-dto';

describe('ProductRequestCreateDto', () => {
  it('should create an instance', () => {
    expect(new ProductRequestCreateDto()).toBeTruthy();
  });
});
