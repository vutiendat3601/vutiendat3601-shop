import { ProductRequestUpdateDto } from './product-request-update-dto';

describe('ProductRequestUpdateDto', () => {
  it('should create an instance', () => {
    expect(new ProductRequestUpdateDto()).toBeTruthy();
  });
});
